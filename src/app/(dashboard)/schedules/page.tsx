"use client";
import { useState } from "react";import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { Clock3,Plus,Power } from "lucide-react";import { toast } from "sonner";
import { PageHeader } from "@/components/layout/page-header";import { Button } from "@/components/ui/button";import { Card } from "@/components/ui/card";import { Dialog,DialogContent,DialogDescription,DialogTitle,DialogTrigger } from "@/components/ui/dialog";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { PageSkeleton } from "@/components/ui/page-skeleton";import { StatusBadge } from "@/components/ui/badge";import { configurationApi,itemsOf } from "@/features/configuration/configuration-api";import type { Schedule } from "@/types/configuration";
const names=["Mon","Tue","Wed","Thu","Fri","Sat","Sun"];const blank={name:"",checkInTime:"08:30",checkOutTime:"17:30",lateGraceMinutes:"10",workingDays:[1,2,3,4,5],timezone:"Africa/Addis_Ababa"};
export default function SchedulesPage(){const qc=useQueryClient();const [open,setOpen]=useState(false);const [editing,setEditing]=useState<Schedule|null>(null);const [form,setForm]=useState<any>(blank);const q=useQuery({queryKey:["schedules"],queryFn:()=>configurationApi.schedules()});const save=useMutation({mutationFn:()=>{const input={...form,lateGraceMinutes:Number(form.lateGraceMinutes)};return editing?configurationApi.updateSchedule(editing.id,input):configurationApi.createSchedule(input)},onSuccess:()=>{toast.success(editing?"Schedule updated":"Schedule created");setOpen(false);setForm(blank);setEditing(null);void qc.invalidateQueries({queryKey:["schedules"]})},onError:(e:Error)=>toast.error(e.message)});const status=useMutation({mutationFn:({s,reason}:{s:Schedule;reason:string})=>configurationApi.scheduleStatus(s.id,!s.isActive,reason),onSuccess:()=>{toast.success("Schedule status updated");void qc.invalidateQueries({queryKey:["schedules"]})},onError:(e:Error)=>toast.error(e.message)});if(q.isLoading)return <PageSkeleton/>;const schedules=itemsOf(q.data??[]);function edit(s:Schedule){setEditing(s);setForm({name:s.name,checkInTime:s.checkInTime,checkOutTime:s.checkOutTime,lateGraceMinutes:s.lateGraceMinutes,workingDays:s.workingDays,timezone:s.timezone??"Africa/Addis_Ababa"});setOpen(true)}return <div className="space-y-6"><PageHeader title="Work schedules" description="Define expected attendance times, grace periods, and working days." action={<Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button onClick={()=>{setEditing(null);setForm(blank)}}><Plus className="size-4"/>Add schedule</Button></DialogTrigger><DialogContent><DialogTitle>{editing?"Edit schedule":"Create schedule"}</DialogTitle><DialogDescription>Employees checking in after the scheduled time plus grace period are considered late.</DialogDescription><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Name"><Input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="Timezone"><Input value={form.timezone} onChange={e=>setForm({...form,timezone:e.target.value})}/></Field><Field label="Check-in"><Input type="time" value={form.checkInTime} onChange={e=>setForm({...form,checkInTime:e.target.value})}/></Field><Field label="Checkout"><Input type="time" value={form.checkOutTime} onChange={e=>setForm({...form,checkOutTime:e.target.value})}/></Field><Field label="Late grace (minutes)"><Input type="number" value={form.lateGraceMinutes} onChange={e=>setForm({...form,lateGraceMinutes:e.target.value})}/></Field><div className="sm:col-span-2"><Label>Working days</Label><div className="mt-2 flex flex-wrap gap-2">{names.map((n,i)=>{const day=i+1;const on=form.workingDays.includes(day);return <button type="button" key={n} onClick={()=>setForm({...form,workingDays:on?form.workingDays.filter((x:number)=>x!==day):[...form.workingDays,day].sort()})} className={`rounded-lg border px-3 py-2 text-sm font-medium ${on?"border-blue-600 bg-blue-50 text-blue-700":"border-slate-200 text-slate-500"}`}>{n}</button>})}</div></div></div><div className="mt-4 rounded-lg bg-slate-50 p-3 text-sm text-slate-600">Late threshold: <b>{form.checkInTime}</b> + <b>{form.lateGraceMinutes} min</b>.</div><div className="mt-6 flex justify-end"><Button disabled={save.isPending} onClick={()=>save.mutate()}>Save schedule</Button></div></DialogContent></Dialog>}/><div className="grid gap-4 lg:grid-cols-2">{schedules.map(s=><Card className="p-5" key={s.id}><div className="flex justify-between gap-4"><div className="flex gap-3"><div className="rounded-lg bg-blue-50 p-2 text-blue-600"><Clock3 className="size-5"/></div><div><div className="flex gap-2"><h2 className="font-semibold">{s.name}</h2><StatusBadge status={s.isActive?"ACTIVE":"INACTIVE"}/></div><p className="mt-1 text-sm text-slate-500">{s.checkInTime} – {s.checkOutTime} · {s.lateGraceMinutes} min grace</p></div></div><Button size="sm" variant="outline" onClick={()=>edit(s)}>Edit</Button></div><div className="mt-5 flex flex-wrap gap-2">{s.workingDays.map(d=><span key={d} className="rounded-md bg-slate-100 px-2 py-1 text-xs font-medium">{names[d-1]}</span>)}</div><div className="mt-5 border-t pt-4"><Button size="sm" variant={s.isActive?"danger":"secondary"} onClick={()=>{const reason=window.prompt(`Reason to ${s.isActive?"deactivate":"activate"} this schedule?`);if(reason)status.mutate({s,reason})}}><Power className="size-4"/>{s.isActive?"Deactivate":"Activate"}</Button></div></Card>)}</div></div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <div className="space-y-1.5"><Label>{label}</Label>{children}</div>}
